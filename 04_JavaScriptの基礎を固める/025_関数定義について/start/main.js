const hello = function(name, age) {
  // let name = 'Code Mafia';
  console.log('hello' + name + age);
  return name + age;
}

hello('tm', 27);
const returnVal = hello('mg', 27)
console.log(returnVal);